- 两数之和：hash 表存值以及对应的下标，一次循环 get target - 当前循环的值
- 字符异位分组：打散 string 后使用 char 数组存储再排序  list.sort(Comparator.comparing(item -> item))，最后用排序后的数组拼接成 key，value 是异位分组的 list
- 最长连续序列：给定一个未排序的整数数组 `nums` ，找出数字连续的最长序列（不要求序列元素在原数组中连续）的长度。请你设计并实现时间复杂度为 `O(n)` 的算法解决此问题。使用 set 判断，当前数字 -1 不在 set 内，则当前数字不断 +1 并且判断相加后的数字是否还在 set 中，并更新最大长度值
- 移动零：从左到右找到第一个等于0的和第一个不为0的，不为0的和等于0的位置交换，不为 0 的  j 需要判断是不是 0 才可以交换
- 盛水最多的容器：左右指针往中间移动，计算最大面积，再判断左右哪边高就移动另一边
- 三数之和：先排序 Arrays.sort(nums)，核心思想，如果当前遍历的值不是第一个，并且当前遍历的值与上一次相同，那么可以跳过，i > 0 && nums[i] == nums[i-1] ，第二轮循环可以有另一个指针从队尾向前遍历，k 移动的条件是 nums[j] + nums[k] > -nums[i]
- 接雨水：先初始化左边最大值数组和右边最大值数组，数组 *leftMax* 和 *rightMax* 其中 i 表示包含当前位置的左边最大值或者右边最大值，leftMax 从 1 开始到 length，rightMax 从 height - 2 开始到  0，leftMax[0] 一定是 0计算当前位置能存多少水，取左右两边较小的值与当前位置高度比较，大于当前则可以存水
- 无重复字符的最长子串：使用 HashSet 如果当前遍历到的 char 已经存在，从 set 中不断移除最左边的字符直到不成立，将当前字符加入 set 中并判断更新长度
- 找到字符串中所有字母异位词：先统计目标数组的字符数使用 26 大小的 int 数组 pCount[item - 'a']++，随后遍历另一个数组，每次截取与目标数组大小相同的字符串并同步更新滑动的数组，两个数组相同则将左边的下标加入结果，数组比较用 Array.equals(int[], int[])
- 和为 K 的子数组：方法一，两遍循环和为 k
- 滑动窗口最大值：方法一，优先队列，PriorityQueue<int[]> pq = new PrioprtyQueue<>(new Compartor<int[]> ( public int compare(int[] pair1, int[] pair2)))，优先比较值的大小，大小相同时比较 index  大小，优先队列存储二元数组。方法二，单调队列用于存贮最大值的下标，前K个数字直接存最大值的下标，循环遍历当前元素大于等于队尾的元素，则可以移除队尾的元素，然后把当前元素加入队尾，随后遍历队头直到队头的元素满足下标 deque.peekFirst() <= i - k 则 ans[i - k + 1] = nums[deque.peekFirst()]
- 最小覆盖子串：new int[128] 用于存储当前字符需要的数量，循环判断 while (r < s.length()) 如果当前的字符串 need 大于 0，则长度 count--, 当前字符的 need --，如果 count 已经为 0 了则判断左边是不是可以往右边，判断的条件是左边的下标小于右边的下标，并且左边当前字符的 need 小于 0，因为如果左边当前的 need = 0 了，左边再移动就不满足条件，移动完左边之后判断 right - left + 1 是不是小于当前 size，如果小于更新长度，随后 left++ 移动一格，right++ 移动
- 最大子数组和：一个数组中子数组和最大的值，方法一：一遍循环，维护一个 max 和 pre 的最大值，pre = max = nums[0]，随后从1 开始遍历，更新 pre 和max 的最大值，pre = Math,max(pre + nums[i], nums[i])，max =  Math.max(max, pre); 方法二：动态规划，以当前数字结尾的最大值数组 dp[i] = Math.max(dp[i - 1] + nums[i], nums[i])，max = Math.max(dp[i], max)
- 合并区间：先按首位数字排序，随后判断当前合并的列表是空或者最大值小于当前数组的左边的值就加入，否则合并，合并后的最大值取当前和已有的更大者
- 轮转数组：方法一：新建一个数组，大小一致，对应 index 满足 index = (i + k) % nums.length 
- 除了自身以外数组的乘积：方法一：分别算出左右两边的乘积，每一位除自身以外的是两边的乘积 方法二：只有一个数组，先算左边再算右边
- 缺失 的第一个正数：方法一：小于等于0的数，都替换成n+1，遍历数组，将当前数字位置小于等于n的数字对应的位置上变成负数，因为可能大一点的位置变成了负数，所以一定要取绝对值判断，最后遍历一遍数组找到第一个大于 0 的数字，则返回他的下标+1，否则返回n+1
- 矩阵置零：分别记录行和列中哪个是 0，mArr 和 nArr 然后循环遍历，只有有一个是 1，该位置就是 0
- 螺旋矩阵：直接按照顺时针判断 left <= right && top <= bottom，先横着走到头，再竖着走到底，然后避免只有一行的情况，需要判断 left < right && top < bottom 才可以走回来
- 旋转图像：关键是列出转移关系，(i, j) -> (j, n - i - 1) -> (n - i - 1, n - j - 1)->(n - j - 1, i)->(i, j)，随后遍历时需要注意奇偶的情况，i < (n + 1)/2，j < n/2
- 搜索二维矩阵：方法一：直接从右上角开始搜索，如果大于目标值向左移动，小于目标值向下移动，方法二：每一行做二分查找
- 相交链表：方法一：走完一个链表后再走另一个链表，节点相同时返回，循环条件注意 while (tempA != tempB) 跳出循环条件为 A B 相等或者都为 null，方法二：哈希 set 判断
- 反转列表：pre = null， curr = head while (curr != null)  temp = curr, curr = curr.next，temp.next = pre pre = temp return pre
- 
